{
  "name": "nct-classificacao",
  "version": "0.1.0",
  "description": "Sistema de classificação para torneios de vôlei de praia — Circuito NCT",
  "main": "main.js",
  "author": "Circuito NCT",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "start": "electron .",
    "test": "ELECTRON_RUN_AS_NODE=1 electron tests/test-chave-dupla.js && ELECTRON_RUN_AS_NODE=1 electron tests/test-fluxo-integrado.js && ELECTRON_RUN_AS_NODE=1 electron tests/test-db.js && ELECTRON_RUN_AS_NODE=1 electron tests/test-repositorios.js && ELECTRON_RUN_AS_NODE=1 electron tests/test-jogos-grupo.js && ELECTRON_RUN_AS_NODE=1 electron tests/test-classificacao.js && ELECTRON_RUN_AS_NODE=1 electron tests/test-mata-mata.js && ELECTRON_RUN_AS_NODE=1 electron tests/test-pontuacao.js && ELECTRON_RUN_AS_NODE=1 electron tests/test-publicacao.js",
    "rebuild": "electron-builder install-app-deps",
    "seed": "ELECTRON_RUN_AS_NODE=1 electron tests/seed-teste.js",
    "postinstall": "electron-builder install-app-deps",
    "dist": "electron-builder",
    "dist:mac": "electron-builder --mac",
    "dist:win": "electron-builder --win",
    "dist:linux": "electron-builder --linux"
  },
  "devDependencies": {
    "electron": "^28.3.3",
    "electron-builder": "^24.13.3"
  },
  "build": {
    "appId": "br.com.nct.classificacao",
    "productName": "NCT Classificação",
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.js",
      "preload.js",
      "package.json",
      "src/**/*",
      "renderer/**/*",
      "assets/**/*"
    ],
    "mac": {
      "category": "public.app-category.sports",
      "target": [
        {
          "target": "dmg",
          "arch": [
            "x64",
            "arm64"
          ]
        }
      ]
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "linux": {
      "target": [
        "AppImage"
      ],
      "category": "Sports"
    }
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "qrcode": "^1.5.4"
  }
}
